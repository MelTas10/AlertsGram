<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Telegram Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #17212b;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .login-container {
            background: #242f3d;
            padding: 30px;
            border-radius: 12px;
            width: 90%;
            max-width: 400px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        .telegram-logo {
            width: 120px;
            height: 120px;
            margin: 0 auto 20px;
            display: block;
        }

        h1 {
            color: #ffffff;
            text-align: center;
            font-size: 24px;
            margin-bottom: 30px;
        }

        .input-group {
            margin-bottom: 20px;
        }

        input {
            width: 100%;
            padding: 12px;
            border: none;
            background: #17212b;
            color: #ffffff;
            border-radius: 8px;
            font-size: 16px;
            box-sizing: border-box;
        }

        input:focus {
            outline: 2px solid #64b5f6;
        }

        .phone-input {
            display: flex;
            gap: 10px;
        }

        .country-code {
            width: 80px;
        }

        .phone-number {
            flex: 1;
        }

        .verification-code {
            letter-spacing: 8px;
            text-align: center;
            font-size: 24px;
        }

        button {
            width: 100%;
            padding: 12px;
            background: #64b5f6;
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #4a9fe0;
        }

        .step {
            display: none;
        }

        .step.active {
            display: block;
        }

        .terms {
            color: #8a9aa7;
            font-size: 13px;
            text-align: center;
            margin-top: 20px;
        }

        .terms a {
            color: #64b5f6;
            text-decoration: none;
        }

    </style>
</head>
<body>
    <div class="login-container">
        <img src="https://telegram.org/img/t_logo.svg" alt="Telegram Logo" class="telegram-logo">
        
        <div class="step active" id="phone-step">
            <h1>Sign in to Telegram</h1>
            <div class="input-group phone-input">
                <input type="text" class="country-code" placeholder="+1" maxlength="4">
                <input type="tel" class="phone-number" placeholder="Phone Number">
            </div>
            <button onclick="showVerification()">Next</button>
            <p class="terms">
                By signing up, you agree to the <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a>
            </p>
        </div>

        <div class="step" id="verification-step">
            <h1>Enter Code</h1>
            <div class="input-group">
                <input type="text" class="verification-code" maxlength="5" placeholder="•••••">
            </div>
            <p style="color: #8a9aa7; text-align: center; margin-bottom: 20px;">
                We've sent the code to your Telegram app
            </p>
            <button onclick="verifyCode()">Sign In</button>
        </div>
    </div>

    <script>
        function showVerification() {
            document.getElementById('phone-step').classList.remove('active');
            document.getElementById('verification-step').classList.add('active');
        }

        function verifyCode() {
            // Здесь можно добавить логику проверки кода
            alert('Verification successful!');
        }
    </script>
</body>
</html>
